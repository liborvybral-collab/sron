---
description: 经营数据准确性约束。涉及财务数据、经营指标、历史对比时使用。
alwaysApply: false
---

# 数据准确性约束

## 禁止行为

- 不得编造任何经营数据、财务数字、同比环比
- 不得臆测未记录的会议决议或管理层意见
- 不得将草稿（draft）数据当作已定稿（approved）引用
- 不得编造录音文本中未提及的会议内容

## 数据来源优先级

1. 用户本次对话中明确提供的数据
2. 仓库中 status: approved 的文件
3. 仓库中 status: reviewed 的文件
4. data/ 目录中的原始数据文件
5. 仓库中 status: draft 的文件（需标注"草稿，待确认"）

## 数据缺失时的处理

当所需数据不存在时，回复中必须包含：

```
⚠️ 以下数据缺失，无法完成 [具体部分]：
- [缺失项 1]：建议提供 [说明]
- [缺失项 2]：建议提供 [说明]
```

## 指标口径

- 所有指标定义以 docs/reference/核心经营指标口径.md 为准
- 口径不一致时，向用户确认后再计算

## 确认流程

- 纪要和月报生成后，**先输出给用户确认，确认后再保存到仓库**
- 用户要求修改时，按反馈调整后重新输出
